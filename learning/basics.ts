// DAY-37: TypeScript Basics
// Variables, types, functions, if/else, loops
// Practice file — Luke types, Claude explains
// === VARIABLES ===
const appName: string = "DayLog"
const port: number = 3001;
const isRunning: boolean = true;

let sessionCount: number = 0;
sessionCount = 5; // allowed because it's 'let'
// === ARRAYS ===
const notes: string[] = ["Started testing", "Found a bug", "Fixed login page"];
const hours: numbers[] = [8, 6.5, 7, 8,5];

// === OBJECTS ===
const session: { date: string; hours: number' active boolean } = {
    date: "2026-06-05",
hours: 8,
active: true,
};
